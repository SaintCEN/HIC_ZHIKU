# port
server.port=

# MySQL
spring.datasource.url=
spring.datasource.username=
spring.datasource.password=
spring.datasource.driver-class-name=

# Spring application name
spring.application.name=

# MyBatis
mybatis.configuration.map-underscore-to-camel-case=
mybatis.type-handlers-package=

# 前端域名（生产环境）
app.frontend.origin=

# 文件上传配置 
file.upload-dir=
app.file.upload-dir=
file.upload.path=
file.upload.url=

# 安全配置
app.security.max-login-attempts=
app.security.lockout-duration=
app.security.session-timeout=
server.error.include-stacktrace=
server.error.include-message=

# servlet
spring.servlet.multipart.max-file-size=
spring.servlet.multipart.max-request-size=

# 编码配置
server.servlet.encoding.charset=
server.servlet.encoding.enabled=
server.servlet.encoding.force=
spring.http.encoding.charset=
spring.http.encoding.enabled=
spring.http.encoding.force=

# HTTP/2支持
server.http2.enabled=

# 日志配置
logging.level.root=
logging.level.com.example.backend=
logging.level.org.springframework=
logging.level.org.springframework.web=
logging.level.org.springframework.security=
logging.level.com.zaxxer.hikari=

# 监控配置
management.endpoints.web.exposure.include=
management.endpoint.health.show-details=

# 性能优化
spring.main.lazy-initialization=
spring.jackson.default-property-inclusion=
spring.jackson.serialization.write-dates-as-timestamps=

# JWT配置
app.jwt.secret=
app.jwt.access-expire-minutes=
app.jwt.refresh-expire-minutes=

# 安全配置
server.ssl.enabled=

# 静态资源配置
spring.web.resources.static-locations=
spring.mvc.static-path-pattern=